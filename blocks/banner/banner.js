export default async function decorate(block) {
  // Banner is a simple text notification bar.
  // The authored content is a single row with a single cell containing a <p>.
  // No DOM restructuring needed.
}
